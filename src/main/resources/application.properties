
#  CONFIGURACIÓN DE LA BD

# URL de conexión a la base de datos MySQL
# jdbc:mysql://localhost:3306/restaurante_pos_db
spring.datasource.url=jdbc:mysql://localhost:3306/restaurante_pos_db?createDatabaseIfNotExist=true

# Usuario de la base de datos (el superadministrador)
spring.datasource.username=root

# Contraseña del usuario root (¡USA LA QUE TÚ CREASTE!)
spring.datasource.password=qwerty123

# Dialecto de Hibernate para MySQL. Le dice a Spring cómo "traducir" Java a SQL para MySQL.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect